// These are only some of the examples
// Full list of operators can be found at https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/operators/

// Basic expression with additive binary operator
int sum = 5 + 5;
Console.WriteLine(sum);

// Basic expression with additive binary operator and variables
int number1 = 5;
int number2 = 5;
int numberSum = number1 + number2;
Console.WriteLine(sum);

// Basic expression with logical not unary opeartor
bool isValid = !false;
Console.WriteLine(isValid);

// Mixed expression with additive and multiplicative operators
var result = 5 + 5 * 10;
Console.WriteLine(result);